import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primaryColor = Color(0xff3240A1);

  //light theme colors
  static const lightPrimaryColor = Color(0xffffffff);
  static const lightSecondaryColor = Color(0xffECECEC);

  //dark theme colors
  static const darkPrimaryColor = Color(0xff0A0A0A);
  static const darkSecondaryColor = Color(0xff292929);
}
